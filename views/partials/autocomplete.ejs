        <script>
      console.log($(window).width())
let testArr = {}
<% let bigBrews = brews%>
<% bigBrews.sort((a, b)=> { %>
<%	if(a.name < b.name) { return -1; } %>
<%    if(a.name > b.name) { return 1; } %>
<%    return 0; %>
<% }) %>
<% bigBrews.forEach((brewery) => { %>
     testArr["<%=brewery.name%>"] = null
    <% }) %>
        
        $('input.autocomplete').autocomplete({
      data: 
        testArr,
        onAutocomplete: () => {
          console.log($(window).width)
          if($(window).width() >992) {
            $('#brewSearch').submit()
          } else {
          $('.littleBrew').submit()
          }
        }
    }
    // onAutocomplete this searches and had to set sizes for it to run so it will search in hamburger form
    
    );
        </script>